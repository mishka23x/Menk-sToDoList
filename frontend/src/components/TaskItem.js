import React from 'react';

function TaskItem({ task }) {
    // Task item logic here
    return (
        <div>
            {/* Task item UI */}
        </div>
    );
}

export default TaskItem;
